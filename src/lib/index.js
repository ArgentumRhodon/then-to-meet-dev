// place files you want to import through the `$lib` alias in this folder.
import { getPeopleData, getSlotData } from "./readers";
import { people, slots } from "./stores";

export {getPeopleData, getSlotData, people, slots}